name: Deploy

on:
  push:
    branches:
      - env/production
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Simulation de déploiement (sans écriture)"
        type: boolean
        default: false

concurrency:
  group: deploy-production
  cancel-in-progress: false

env:
  PHP_VERSION: "8.1"
  NODE_VERSION: "24"
  THEME_PATH: web/app/themes/cgibelli

jobs:
  deploy:
    name: Build & deploy (production)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # ------------------------------------------------------------------
      # Validation des secrets
      # ------------------------------------------------------------------
      - name: Vérifier les secrets requis
        env:
          SSH_HOST: ${{ secrets.SSH_HOST }}
          SSH_USER: ${{ secrets.SSH_USER }}
          FTP_PASSWORD: ${{ secrets.FTP_PASSWORD }}
          DEPLOY_PATH: ${{ secrets.DEPLOY_PATH }}
        run: |
          missing=""
          for name in SSH_HOST SSH_USER FTP_PASSWORD DEPLOY_PATH; do
            [ -z "${!name}" ] && missing="$missing $name"
          done
          if [ -n "$missing" ]; then
            echo "::error::Secrets manquants :$missing"
            exit 1
          fi

      # ------------------------------------------------------------------
      # PHP / Composer
      # ------------------------------------------------------------------
      - name: Setup PHP ${{ env.PHP_VERSION }}
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ env.PHP_VERSION }}
          extensions: simplexml, json, intl, curl, mbstring
          tools: composer:v2
          coverage: none

      - name: Valider composer.json / composer.lock
        run: composer validate --no-check-publish

      - name: Cache Composer
        uses: actions/cache@v4
        with:
          path: ~/.cache/composer/files
          key: composer-${{ env.PHP_VERSION }}-${{ hashFiles('composer.lock') }}
          restore-keys: composer-${{ env.PHP_VERSION }}-

      - name: composer install (prod)
        run: |
          composer install \
            --no-dev \
            --prefer-dist \
            --no-interaction \
            --no-progress \
            --optimize-autoloader

      # ------------------------------------------------------------------
      # Build du thème (Webpack)
      # ------------------------------------------------------------------
      - name: Setup Node ${{ env.NODE_VERSION }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: yarn install
        working-directory: ${{ env.THEME_PATH }}
        run: yarn install --frozen-lockfile --non-interactive

      - name: Build production (webpack)
        working-directory: ${{ env.THEME_PATH }}
        run: yarn production

      - name: Vérifier le résultat du build
        working-directory: ${{ env.THEME_PATH }}
        run: |
          for artifact in assets/scripts/app.js assets/styles/styles.css; do
            if [ ! -s "$artifact" ]; then
              echo "::error::Build incomplet, fichier absent ou vide : $artifact"
              exit 1
            fi
          done
          echo "Assets générés :"
          du -sh assets/*

      # ------------------------------------------------------------------
      # Déploiement via FTPS (Port 21 sécurisé)
      # ------------------------------------------------------------------
      - name: 🚀 Déployer les fichiers via FTP sécurisé
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.SSH_HOST }} # Réutilise blizz.o2switch.net
          username: ${{ secrets.SSH_USER }} # Réutilise ton user chgi3340
          password: ${{ secrets.FTP_PASSWORD }}
          port: 21
          server-dir: ${{ secrets.DEPLOY_PATH }}/ # Envoie directement dans ton dossier de prod
          dry-run: ${{ inputs.dry_run }}
          # On traduit ton fichier d'exclusions rsync pour l'action FTP
          exclude: |
            **/.git*
            **/.git*/**
            **/.vscode/**
            **/.idea/**
            **/.claude/**
            **/node_modules/**
            **/*.log
            **/web/app/themes/cgibelli/_src/**
            **/web/app/themes/cgibelli/package.json
            **/web/app/themes/cgibelli/yarn.lock
            **/web/app/themes/cgibelli/webpack.config.js
            **/web/app/themes/cgibelli/postcss.config.js
            **/.env
            **/.env.*
            **/wp-cli.local.yml
            **/.htaccess
            **/app/uploads/**
            **/app/cache/**
            **/app/upgrade/**
            **/app/languages/**
            **/app/advanced-cache.php
            **/app/object-cache.php
            **/app/db.php
            **/app/wp-rocket-config/**